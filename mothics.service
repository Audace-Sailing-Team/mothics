[Unit]
Description=Mothics CLI Service
After=network.target

[Service]
User=%i
WorkingDirectory=%h/mothics
ExecStart=/bin/bash %h/mothics/cli.sh
ExecStop=/usr/bin/tmux kill-session -t mothics
Restart=on-failure
Type=forking

[Install]
WantedBy=multi-user.target

